# BEGIN REMINDER API
server {
    listen 80;
    server_name reminder-api.wwmty.com;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type "text/plain";
        try_files $uri =404;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    http2 on;
    server_name reminder-api.wwmty.com;

    ssl_certificate /etc/letsencrypt/live/reminder-api.wwmty.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/reminder-api.wwmty.com/privkey.pem;
    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:10m;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    client_max_body_size 6m;
    resolver 127.0.0.11 ipv6=off valid=30s;
    resolver_timeout 5s;

    location / {
        set $reminder_upstream reminder-backend:8080;
        proxy_pass http://$reminder_upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        proxy_connect_timeout 10s;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
    }
}
# END REMINDER API
