# Reminder 服务器部署手册

Reminder 后端现在是单 Maven 项目、单 Spring Boot JAR、单容器。发布链路与 saas-admin 保持一致：Jenkins → Maven(Java 17) → Docker → 内部镜像仓库 → Docker Compose → Nginx HTTPS。

Reminder 复用 saas-admin 的 PostgreSQL 与 Redis 容器，并通过独立 PostgreSQL schema 和 Redis 逻辑库隔离数据。文件上传通过 saas-admin 的 APP_CLIENT 审计接口进入 MinIO，Reminder 不保存 MinIO 账号。

## 1. 已准备的审计身份

- saas-admin 中已存在启用状态的 `app_code=reminder` APP_CLIENT。
- Jenkins 全局凭据 `reminder-saas-storage-app` 已保存该 AppID/Secret；类型为 Username with password，其中 username 是 AppID。
- `/auth/app-login` 和 `/sys/storage/direct-upload-target` 已验证，存储对象作用域为 `app/reminder/`。

不要把 AppID、Secret 或 saas-admin 的 MinIO 凭据写入 Git、Compose 文件或构建日志。轮换 APP_CLIENT 后只更新 Jenkins 凭据。

## 2. 首次部署前准备

### 2.1 DNS、TLS 与微信合法域名

1. 将 `reminder-api.wwmty.com` 解析到 SaaS 服务器。
2. 申请证书并放到：
   - `/etc/letsencrypt/live/reminder-api.wwmty.com/fullchain.pem`
   - `/etc/letsencrypt/live/reminder-api.wwmty.com/privkey.pem`
3. 安装 `reminder-backend/deploy/nginx/reminder-api.conf`，执行 `nginx -t` 后 reload。
4. 在微信公众平台加入以下合法域名：
   - request/uploadFile：`https://reminder-api.wwmty.com`
   - downloadFile：saas-admin `storage.minio.public-base-url` 实际使用的 HTTPS 域名

saas-admin 必须配置稳定的 `storage.minio.public-base-url`。不能让用户头像长期保存为一小时有效的 MinIO 预签名 URL。

### 2.2 共享 PostgreSQL 与独立 schema

- PostgreSQL 使用 `saas-postgres:5432` 中的 `saas-admin` 数据库；Reminder 所有业务表和 Quartz 表固定放在独立 schema `reminder`。
- 连接用户使用 `pguser`，数据库密码只放入 Jenkins 的 `reminder-runtime-env` Secret file，不写入 Git 或镜像。

仅首次、且 `reminder` schema 尚未包含任何业务表或 Quartz 表时，依次执行：

```bash
docker exec -i saas-postgres psql -v ON_ERROR_STOP=1 -U pguser -d saas-admin < reminder-backend/src/main/resources/schema.sql
docker exec -i saas-postgres psql -v ON_ERROR_STOP=1 -U pguser -d saas-admin < reminder-backend/src/main/resources/quartz.sql
```

两个 SQL 文件都会显式切换到 `reminder` schema，并在发现已有对应表时拒绝执行。禁止把它们当成已有生产数据的升级脚本；已有数据时必须先备份并改用迁移 SQL。

### 2.3 Redis 与网络

- Redis 使用 `saas-redis:6379`；Reminder 固定使用逻辑 `DB 9`，不得清理其他逻辑库。
- Reminder Compose 只加入已有外部网络 `saas-app`，通过容器名访问 `saas-postgres`、`saas-redis`、`saas-nacos` 和 `saas-admin-backend`。
- 宿主机只开放 `127.0.0.1:18080` 给 Nginx，不直接暴露 Java 端口。

### 2.4 Jenkins 运行配置

除已创建的 `reminder-saas-storage-app` 外，再创建 Secret file 类型凭据 `reminder-runtime-env`。内容参考 `reminder-backend/deploy/.env.example`，但不要在该文件中重复填写 `SAAS_STORAGE_APP_ID` 和 `SAAS_STORAGE_SECRET_CODE`；流水线会从独立审计凭据注入。

至少配置：

- PostgreSQL URL、`pguser` 用户、密码和 `DB_SCHEMA=reminder`
- Redis 地址、`REDIS_DATABASE=9` 和密码
- 使用 `openssl rand -base64 64 | tr -d '\n'` 生成单行 JWT Secret（必须是 Base64）
- 微信小程序 AppID/Secret
- 前端 URL 与 CORS 来源
- 可选的 Nacos、订阅消息、邮件和腾讯 TTS 配置

Jenkins 任务的 Script Path 使用 `reminder-backend/Jenkinsfile`。镜像仓库凭据继续使用现有 `nexus-repo-credentials`。

首次运行流水线前，在 SaaS 主机的 Jenkins 已挂载持久卷中创建状态目录：

```bash
sudo install -d -m 700 /opt/saas-app/reminder/deploy-state
```

该目录会以 `600` 权限保留每次成功发布的运行环境、Compose 和镜像版本，用于原子回滚与审计；其中包含运行密钥，禁止纳入普通日志、Git 或非加密备份。

## 3. 发布

Jenkins 参数：

- `branch`：要发布的分支
- `version`：不可变镜像版本，例如 `1.0.0`

流水线会执行全量测试和打包、构建并推送 `127.0.0.1:3000/admin/reminder-backend:<version>`，然后只更新 Compose 服务 `reminder-backend`。部署脚本等待容器内 `/actuator/health`；失败时只回滚 Reminder 到上一组镜像、运行环境和 Compose，不重启 saas-admin 或任何中间件。

手工演练可使用：

```bash
export REMINDER_ENV_FILE=/secure/path/reminder.env
export REMINDER_IMAGE=127.0.0.1:3000/admin/reminder-backend:1.0.0
export REMINDER_STATE_DIR=/opt/saas-app/reminder/deploy-state
bash reminder-backend/deploy/deploy-reminder.sh
```

## 4. 验收

```bash
docker ps --filter name=reminder-backend
docker inspect --format '{{.State.Health.Status}}' reminder-backend
docker logs --tail 200 reminder-backend
curl -fsS https://reminder-api.wwmty.com/actuator/health
```

还需完成一次真实闭环：微信 `wx.login` → HTTPS `/api/auth/wechat/login` → JWT 请求 → 头像上传 → 返回 HTTPS URL，并在 saas-admin 审计中确认对象 key 位于 `app/reminder/`。

## 5. 切换与回滚

1. 先部署 Reminder 容器，不停止原微信云托管。
2. 完成健康检查、真实微信登录、提醒创建/触发、头像上传与审计验证。
3. 发布使用新 `VITE_API_BASE_URL` 构建的小程序/H5，并观察日志。
4. 导出旧微信云文件，通过受审计的存储接口迁移并更新数据库 URL。
5. 观察期通过后再停旧云托管。

应用回滚由部署脚本恢复上一组镜像、运行环境和 Compose；若已有容器但缺少审计状态，脚本会在变更容器前拒绝发布。前端回滚到上一审核版本。旧云托管只能在数据迁移和新链路验证完成后关闭。
